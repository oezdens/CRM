# Project

This is a Vite + React + TypeScript website project.

## Quick start (local)

Requirements: Node.js (LTS) and npm or pnpm/yarn.

```powershell
# install deps
npm install

# start dev server
npm run dev

# build for production
npm run build
```

If you host the site elsewhere, follow your provider's deployment instructions.
